// ClubZap API type definitions — membership and payments scope.
// Expand this file once the API key is issued and endpoint docs are confirmed.

export interface ClubZapCredentials {
  appId: string;
  jwt: string;
  clubId: string;
  baseUrl: string;
}
